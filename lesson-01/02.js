// code
let number1 = Math.floor(Math.random() * 100);

//const checkRandNumForOdd = (num) => num % 2 ? `${num}:Это нечетное число` : `${num}:Это четное число`;

console.log(number1 % 2 ? `${number1}:Это нечетное число` : `${number1}:Это четное число`);